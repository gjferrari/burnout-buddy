{{!-- This handlebar focuses on the activty aspect of the app. Will have chosen activity + activity options --}}
    {{!-- BOOTSTRAP used in this handlebar template --}}

{{!-- These cards focus on the activity cards available to you --}}
<div class="row">

  {{!--Mindfulness--}}
  <div class="col-sm-6">
    <div class="card">
        <div class="card-header">{{!--Activity Category--}}</div>
      <div class="card-body">
        <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
        <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
        <a href="#" class="btn btn-primary" id="btn">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
      </div>
    </div>
  </div>

  {{!--Movement--}}
  <div class="col-sm-6">
    <div class="card">
        <div class="card-header">{{!--Activity Category--}}</div>
      <div class="card-body">
        <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
        <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
        <a href="#" class="btn btn-primary" id="btn">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
      </div>
    </div>
  </div>

  {{!--Outreach--}}
  <div class="col-sm-6">
    <div class="card">
        <div class="card-header">{{!--Activity Category--}}</div>
      <div class="card-body">
        <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
        <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
        <a href="#" class="btn btn-primary" id="btn">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
      </div>
    </div>
  </div>

  {{!--Distract--}}
  <div class="col-sm-6">
    <div class="card">
        <div class="card-header">{{!--Activity Category--}}</div>
      <div class="card-body">
        <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
        <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
        <a href="#" class="btn btn-primary" id="btn">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
      </div>
    </div>
  </div>

  {{!--Create--}}
  <div class="col-sm-6">
    <div class="card">
        <div class="card-header">{{!--Activity Category--}}</div>
      <div class="card-body">
        <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
        <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
        <a href="#" class="btn btn-primary" id="btn">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
      </div>
    </div>
  </div>

  {{!--Rest--}}
  <div class="col-sm-6">
    <div class="card">
          <div class="card-header">{{!--Activity Category--}}</div>
      <div class="card-body">
        <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
        <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
        <a href="#" class="btn btn-primary" id="btn">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
      </div>
    </div>
  </div>
</div>





{{!-- Future Development: Own activity page? --}}
    {{!-- This card focuses on the chosen activity --}}
        {{!-- future development (not MVP) card could be changed to encompass a picture as well? --}}
        {{!-- <div class="card text-center">
        <div class="card-body">
            <h5 class="card-title">{{!-- ACTIVITY TITLE --}}</h5>
            <p class="card-text">{{!-- ACTIVITY DESCRIPTION --}}</p>
            <a href="#" class="btn btn-primary">{{!-- ACTIVITY START BUTTON (this button will lead into the timer (javascript)) --}}</a>
        </div>
        </div> --}}